# HANDOFF — last updated: 2026-08-19

## 今どこ(現在の作業対象)

**Phase 0 から Phase 3 まで成立(2026-08-19)。本プロジェクトの中核は動いている。**

アプリの印刷ダイアログから印刷すると、MD-5000 が **白 + 黒の 2 パスを 1 ジョブで、
見当が合った状態で刷る。** §11.5(全パスを 1 ジョブとして送れるか)が肯定で決着した。

先行機は **MD-5000(パラレル + USB 変換ケーブル、生送出)**(D-025)。
MD-5500 は給紙機構の異常で退役中(修理を検討中)。

### 成立していること

- **L0(送出)** — 送出方式は 2 系統。パケット層(MD-5500 / USB。§15.2)と
  **生送出**(MD-5000 / パラレル。D-025)。生送出では **応答を読んではならない**。
  機種→(プロファイル, 送出方式, VID)の対応は `src/Foilwright.Core/MachineRoute.cs` に集約
- **L1 / L2** — golden 17 種とバイト一致。**`ref/`(Python)と `src/Foilwright.Core`(C#)の両方**で通る
- **状態読み出し** — `05 01` → 38 バイト。装填カセットと動作状態が読める(§11.4)。
  **機構エラーの種別は識別できない**(§11.4 の訂正。1 例で確定と書いたのが誤りだった)
- **印刷ダイアログ経路** — 仮想プリンタ「Foilwright MD-5500」(in-box PostScript ドライバを間借り。D-022)
  → Local Port → 名前付きパイプ(D-023)→ トレイアプリ → Ghostscript → プレーン分離 → 生送出
- **トレイアプリ** — プレビュー・設定二層(既定値 + ジョブごとの上書き。D-024)・カセット過不足判定
- **カール矯正の抑制** — デカール用途で必須(§10.10.4)。設定とチェックボックスから選べる。
  golden g17 で「差分は curl バイト 1 箇所だけ」を確認済み
- **見当合わせ** — 紙の上部・中央・下部のいずれでも、ずれは目視で検出できなかった(§15.9)

### 埋めていない値(推測で埋めないこと)

- `profiles/md-5000.yaml` の **`lf_correction` は `null` のまま。**
  補正が不要と確認できただけで、**0 が正しいと確定したのではない**(§5.2 / §15.9)
- `max_width_dots` も未実測
- **ベースドホワイトの色選択コード**(候補 `0x1c` / `0x1e`)— カセット未所有のため確定できない

## 実機なしで到達した範囲(2026-08-05)

**MD-5000 を繋げばすぐ試せる状態まで実装済み。** テスト 63 件パス、golden 17 種一致は維持。

| 層 | 状態 |
|---|---|
| L0 送出 | **2 系統**。パケット層(MD-5500 USB)と生送出(MD-5000 パラレル)。`--machine` で切替 |
| L0 保護 | タイムアウト・ドレイン・応答の中身検査。**生送出は応答を読まない**(ウェッジ回避) |
| L1 emitter | golden 17 種一致。機種差はプロファイルに閉じる |
| L2 raster | インク指定方式 3 種(既定 `auto`)。二役インクの合成、空プレーンの除外 |
| L3 変換器 | パイプ受信 → Ghostscript → 切り出し → プレーン → RGL → 送出 |
| 状態デコーダ | 38 バイトの応答を純正と同じ語彙の日本語へ(§13.8.3)。**確定エラーは 1 件のみ、残りは生値表示** |
| トレイアプリ | **初版あり**。トレイ常駐・パイプ受信・**インク割り当てプレビュー**・設定二層・印刷/取消 |

### トレイアプリの現状(2026-08-07)

`src/Foilwright.Tray/`(WinForms / net10.0-windows)。

**実機なしでプレビューを確認できる:**

```
cd src
dotnet build
./Foilwright.Tray/bin/Debug/net10.0-windows/Foilwright.Tray.exe --debug-preview-png <入力.ps> <出力.png>
```

`dumps/pipe_out.ps`(マジックカラー 6 色のテストページ)を与えると、**6 パス・6 インクが色分けで描画される**ことを確認済み。**白は青の市松模様**で表示される — 背景に埋没して誤爆を見逃さないため(§7.2)。

**市松の位相は必ずプレビュー側の座標で決めること。** 元画像の座標で決めると、縮小のサンプリングで奇偶が規則的に間引かれ、市松が単色に潰れる(実測で確認・修正済み)。

**設定項目は §7.1 のすべてを実装済み。** インク指定方式 / 機種 / 解像度 / メディア種別 / ハーフトーン / 白版モード。
保存先は `%AppData%\Foilwright\settings.json`(既定値)、プレビュー画面でジョブごとに上書きできる(D-024 の二層)。

**白版モードは 3 択(D-027):**

| 設定 | 動作 | 実測(6 色テストページ) |
|---|---|---|
| `none` | 白のプレーンを作らない | **5 パス**(白が消える) |
| `auto` | 他インクが乗る画素の和集合 | 6 パス |
| `magic` | 白の `magic_rgb` 一致分のみ | 6 パス(中身は auto と別) |

**解像度 1200x600 に対応済み。** 幅方向だけ 2 倍になる(`papers/5000-series.yaml` の換算規則)。
Ghostscript も `-r1200x600` の非正方でラスタライズする。**A4/1200x600 は 9600x6372 で、変換に約 2 分かかる。**

**カセット過不足の判定も実装済み**(§7.3 の手順 1〜3。4〜5 は実機待ち。理由は §7.3 に記載)。

**未実装:**

- §7.3 の手順 4〜5(途中停止して交換を待ち、検証して続きを送る)。**実機で待機可能時間を測るまで作らない**
- 実機での検証全般(プリンタが 2 台とも給紙エラー)


**未検証で残っているもの(実機が要る):**

- パラレル経路で状態問い合わせ(`05 01`)が通るか
- 生送出で大きいジョブが一括で通るか(分割の要否)
- 印字位置の切り出しが正しいか(位置確認ページ `tools/print-position-test.ps1` で測る。
  左上の L 字が紙の左端から約 32.8mm・上端から約 31.8mm に出れば正しい)

## MD-5000 で再開するときの手順(2026-08-05 / D-025)

**MD-5500 は給紙機構の異常で使用不可。先行機を MD-5000(パラレル + USB 変換ケーブル)へ戻した。**

### 1. 接続したらまず VID を確認する

```
"C:\Program Files\usbipd-win\usbipd.exe" list
```

記録では変換ケーブル(ELECOM UC-PGT)は **`056E`**(§10.1.1)。**個体差・製品差がありうるので実測を優先する。**
違っていれば `--vid` で上書きする。

### 2. 状態問い合わせを試す(通らなくてもよい)

```
cd src
dotnet run --project Foilwright.Cli -- status --machine md-5000
```

**`--machine` を付けると、プロファイル・送出方式・VID がまとめて切り替わる**(`MachineRoute.cs` の対応表)。
既定は `md-5000`。VID が違えば `--vid 1234` で上書きできる。

**パラレル経路で `05 01` が通るかは未検証。** 通らなくても送出はできる設計になっている。
失敗した場合はエラーメッセージを記録して先へ進む。

### 3. 小さいジョブから送る

```
dotnet run --project Foilwright.Cli -- print ..\dumps\phase1_blackraster.bin --machine md-5000
```

**生送出は分割せず一括で書く。** 大きいジョブが一括で通るかは未検証(§15 / D-025)。
**必ず小さいジョブ(1488 バイト)から確認する。**

### 4. 7 月の失敗を再検証する

D-017 の根拠だった「MD-5000 + 変換ケーブルでデータが到達しない」(§11.1.1)は、
**「読み残しが滞留して会話がずれる」問題を知らなかった時期の観測**である(§11.4 の訂正)。
現在はドレイン・タイムアウト・応答の中身検査を実装済みのため、**同じ構成でも結果が変わりうる。**

同じ症状が再現した場合は、**当時と違って原因を切り分ける道具が揃っている**:

- `DrainedByteCount` が 0 でなければ読み残しがあった証拠
- タイムアウトで固まらず理由が表示される
- 応答の中身(`02 80 21` で始まるか)を検査している

### 5. 実機作業の鉄則(今日の事故から)

- **送出中に別プロセスから状態を問い合わせない**(§15.2.1。会話は 1 本しかない)
- **応答を返さない操作の直後に読まない**(ウェッジし物理再接続でしか回復しない)
- 採取ツール(`tools/probe-status.ps1`)は**トレイアプリ/listen が動いていないときだけ**使う
- エラーが出たら**まずエラーの解消**。エラー中のカセット情報は更新されておらず信用できない(§11.4)

## 実機作業の作法(重要)

- **usbipd で bind したら、使い終わったら必ず unbind する。** 2026-08-04 は朝の解析で bind したまま一日放置し、Windows ネイティブ経路が不調になった際の切り分けを濁らせた。`usbipd list` が `Shared` のままなら `tools/usbip-unbind.cmd` で解除する
- **USBPcap は USB フィルタドライバであり、VMware の USB パススルーと併用できない**(2026-08-08 実測。デバイスがホストからも仮想 PC からも消えた)。usbipd も同ソフトを「非互換」と警告する。**採取が終わったら必ずアンインストールして再起動する。** 入れっぱなしにすると、後日の不調で容疑者として立ちはだかる
- **仮想 PC でプリンタを使った後はホスト側の状態を確認する。** VMware がデバイスを掴んで離すと再列挙され、ポート番号(USB002 → USB003 等)が変わる
- **バルク IN を読んでよいのは応答を返すコマンドの直後だけ。** 空読みするとインターフェースがウェッジする
- ウェッジからの回復は `usbipd detach` → `attach`(物理再接続は不要。約 10 秒)
- USBPcap 採取は `tools/capture-usb.cmd` を管理者で実行。**古い 0 バイトの pcap が残っていると失敗する**ので先に消す。バッチは ASCII のみ(日本語コメントは cp932 で化ける)
- `.ps1` は **UTF-8 BOM + CRLF** で保存する。BOM が無いとインライン C# 中の日本語コメントが壊れる
- **Python でリポジトリのファイルを書くときは `write_bytes` を使う。** `write_text` は `\n` を CRLF に変換し、差分が全行置換になる(今日 2 回やった)
- bash の heredoc に**バックスラッシュやバッククォートを含む Python を書かない**。Write ツールでスクリプトファイルを作ってから実行する

## この PC の現在の状態

- 仮想プリンタ **「Foilwright MD-5500」** — in-box の PostScript ドライバ、ポートは `\\.\pipe\foilwright`
- 署名実験の証明書とドライバパッケージは**すべて削除済み**(信頼ストアに残していない)
- Ghostscript 9.53.3 導入済み、.NET 10 SDK 導入済み

## 完了したこと(このセッション)

- **ref/ の L1/L2 が完成。** golden 15 種とバイト一致
  - L2 の入口 3 方式(`auto` / `per_page` / `spot_only`)。D-016
  - ハーフトーン 3 方式(none / halftone / coarse_halftone)
  - 設定ファイル層(profiles / palette / papers / media)。D-013 / D-014
  - マジックカラーの判定規則を確定(D-015。整数演算のみ)
- **Phase 0 を 2 機種で完了。** MD-5000 + ELECOM UC-PGT(§10.1.1)と MD-5500 の USB 直結(§10.1.0)。どちらも双方向通信が成立
- **§11.3 / §11 #8 が解決。** MD-5500 の USB 直結は標準 `usbprint.sys` が掴む「楽なケース」で、L0 の追加実装は不要
- **Phase 1 を 2 機種で実施 → いずれも未達。** 症状が完全に一致したため、原因の見立てを機械側からバイト列へ変更(§11.1.1)
- 外部の実践知見を 3 件記録(§10.8 / §10.9 / §10.10)。ホワイト 2 種類の性質、剥離を防ぐ層構成、バーコードの 2 層構造、サードパーティインクの運用など
- **日本語版マニュアルを入手し §13.5 に記録。** 公式ドライバが専用ポート `MD_LPT1:` を使う設計であること、国内版のインク名称が海外版と異なること(palette の label を修正)など
- DOMAIN が 0.1.0 → 0.2.27-draft、DECISIONS が D-017 まで

## 未完了 とその理由 / 詰まっている点

**Phase 1 が通っていない。バイト列を疑う段階。**

- **2 機種 x 別経路で同一症状。** MD-5000(変換ケーブル/VID 056E/USB002)と MD-5500(直結/VID 044E/USB003)のどちらも、全量送出・無反応・エラーなし
- **転送モードは原因ではない(検証済み)。** blackRaster(0x00)で送っても変わらなかった
- **MD-5500 が異音で停止中。** セルフテスト実行時に発生。原因未特定。リボンを外すとエラーは解消した
- **公式ドライバの出力を捕捉できていない。** ポートを FILE: にすると保存ダイアログは出るが 0 バイト。ドライバが専用ポート MD_LPT1: を前提としており、存在確認に失敗して打ち切っているとみられる(§13.5.1)
- **エラー原因はホストから読めない**(§11.4)。3 経路とも未達 — ReadPrinter は Win32 error 6、ポート直接オープンは失敗、**usbprint.sys の IOCTL は開けるが値が固定**(異音時・リボン除去後・再接続後・カバー開放時のすべてで 0x01)。ただし **CreateFile でデバイスを開けた**のは成果で、L0 の作り直しは不要かもしれない
- **汎用ドライバ経由では Windows もエラーを検知しない。** カバー開放時も DetectedErrorState=2(エラーなし)のまま
- **ベースドホワイトの色選択コードが不明**(§11 #11)。MD-5000 用ドライバには選択肢が存在しないことを確認済み。品番は MDC-OPWH

## 次セッションの推奨着手点

Phase 3 が成立したので、ここからは**実用化の詰め**になる。

0. **前提** — デカールを刷るときは **「カール矯正を止める」にチェックを入れる**(§10.10.4)。紙が反ると転写できない
1. **本命の用途を通す** — ウォータースライドデカール(アイデカール)を 1 枚、
   実際のデザインで最後まで刷る。専用紙・特色・重ね順が実物で噛み合うかは、
   検査図では分からない
2. **版ずれの定量測定** — §15.9 は「目視で検出できない」までしか言えていない。
   細線や細かい市松を刷って、何ドットまで許容できるかを数字にする
3. **カセット交換の待機可能時間を測る** — §7.3 の手順 4〜5(途中停止して交換を待ち、
   検証して続きを送る)は、**実機で待機可能時間を測るまで作らない**。
   何秒でタイムアウトするかが分からないと設計できない
4. **金・銀の特色を実機で通す** — golden ではバイト一致済みだが、実物は未確認

### カセット入手待ち

- **ベースドホワイト** — 色選択コードの確定(候補 `0x1c` / `0x1e`)

### 未解明のまま残っているもの

- **機構エラー 8 種の識別** — 状態バイトでは区別できない(§11.4)。
  USBPcap 経路も塞がった(**USBPcap と VMware の USB パススルーは併用不可**。実測)
- `rec[9]` / `rec[10]` の完全な意味、カセットレコードの 2〜3 バイト目
- **リボン残量の解釈** — 純正で 1 枚刷った前後で値が非単調に変化したため撤回済み。
  §7.2 の残量表示は保留

### MD-5500

給紙機構の異常。紙を入れても給紙せず、エラーランプが点滅する。修理を検討中。
**MD-5500 が直っても先行機は MD-5000 のまま**(D-025)。

## このセッションで下した設計判断

D-013(YAML)/ D-014(用紙表の分離)/ D-015(色マッチング)/ D-016(インク指定 3 方式)/
D-017(先行機を MD-5500 の USB 直結へ変更。D-012 を差し替え)
→ DECISIONS.md に追記済み

2026-08-03 は設計判断なし(調査と実装のみ)

## 触ると危険な箇所 / 現在の一時的回避策

- `tests/golden/*.bin` は基準。**絶対に書き換えない**
- `tools/send-raw.ps1` は **UTF-8 BOM + CRLF が必須**。PowerShell 5.1 が BOM なしを Shift_JIS として読み、here-string は CRLF でないと解釈されない
- プリンタキューを 2 つ作成済み。不要なら `Remove-Printer` で削除可
  - `Foilwright-Test`(Generic / Text Only / USB002 / MD-5000 + 変換ケーブル)
  - `Foilwright-MD5500`(Generic / Text Only / USB003 / MD-5500 直結)
- WSL が頻繁に落ちる(HCS タイムアウト)。`wsl --shutdown` → 管理者 PowerShell で `Restart-Service vmcompute` の順で復旧
- **ジョブが `Retained` で残る**。次の送出前に `Remove-PrintJob` で消さないと切り分けが濁る
- **DOMAIN の変更履歴は必ずファイル末尾に追記**。表の途中に挿入すると版の順序が崩れる(今回 2 回やり直した)
- **bash 経由で PowerShell/Python に文字列を渡すとバッククォートが展開される**。識別子を含む文書を書くときは Edit ツールを使う(コミットメッセージで 1 回被害)
- **バックスラッシュを含む文字列(デバイス ID など)を bash 経由の Python heredoc で書かない。** `\6` が 8 進エスケープとして解釈され制御文字 `\x06` になる事故が発生(2026-08-01)。表示上は見えないため気づきにくい。**Edit ツールを使うか、`chr(92)` で組み立てる**
- **`git add -A` を使わない。** 追加するファイルを明示すること。2026-08-01 に著作物(MD-5000 マニュアル PDF)を公開リポジトリへ誤コミットし、`git filter-repo` + force push で履歴から除去する事故を起こした。`.tokensave/` も同じ原因で一度混入しかけている。**公開リポジトリでは事故の代償が大きい**(DOMAIN §12.6 / §12.7)
- **履歴を書き換える前に必ず `git bundle create` でバックアップを取る。** `--force` は防護フックが止めるので `--force-with-lease` を使う。filter-repo はリモート設定を削除するため、実行後に `git remote add` と `git fetch` が必要

## 検証コマンド(このプロジェクトで実際に使った手順)

```powershell
.venv\Scripts\python.exe -m pytest ref/tests/ -v     # 76 件
.venv\Scripts\ruff.exe check ref/
wsl -e bash tests/cases/make_golden.sh               # golden 再生成(WSL 必須)
```

実機への送出(Phase 1):

```powershell
powershell -ExecutionPolicy Bypass -Command "& 'E:/build/Foilwright/tools/send-raw.ps1' -Path 'E:/build/Foilwright/dumps/phase1_5mm_black.bin' -PrinterName 'Foilwright-Test'"
```

`-WhatIf` を付けると送信せず内容確認のみ。
